# Solo se usa con `npm run dev` (servidor de desarrollo de Vite). En Docker, la URL de
# la API se resuelve en runtime vía public/config.js (ver docker-entrypoint.sh).
VITE_API_BASE_URL=http://localhost:8000
