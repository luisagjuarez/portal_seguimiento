# Bitácora — 2026-08-26

## Verificación visual de la migración a rutas reales (pendiente del 2026-08-25)

Se completó en navegador el checklist de verificación de la migración a `react-router-dom`
dejado pendiente el 2026-08-25 (`00_ARCHIVOS/BITACORAS/2026-08-25.md`), ahora que la extensión
Claude-in-Chrome sí conectó (el usuario reinició Chrome). Se levantó `npm run dev` (puerto
5173, detenido al cerrar la sesión) y, para el punto 7, también el mailserver de pruebas
(`docker compose --profile local-test up -d mailserver`, detenido al terminar — no estaba
corriendo antes de esta sesión).

**Los 8 puntos del checklist pasaron sin hallazgos, no hizo falta ningún cambio de código:**
1. Login (`DOVELA_LG`, Scrum Master) y navegación por cada opción del sidebar → la URL cambia
   acorde (`/chat`, `/solicitudes`, `/tablero`).
2. `/solicitudes/7` → F5 sigue mostrando el detalle correctamente (el punto central de la
   migración).
3-4. Tarea abierta desde el Tablero (`/tareas/34`) → "Regresar" navega a la solicitud padre
   (`/solicitudes/26`), igual que desde el detalle de una solicitud — mismo botón cubre ambos
   casos.
5. "Cambiar contraseña" del header → `/cambiar-password`; "Cancelar" vuelve a `/`.
6. Cerrar sesión → vuelve a `/` con login, sin sidebar.
7. Recuperación de contraseña de punta a punta: `POST /api/auth/forgot-password` para
   `DOVELA_WA`, leído el correo real por IMAP contra el mailserver de pruebas (`localhost:3143`,
   auth deshabilitada) con un script Python de un solo uso, extraído el link
   `/?reset_token=...`, completado el reset y confirmado el login con la nueva contraseña. Se
   dejó la contraseña de `DOVELA_WA` en el mismo valor que ya tenía documentado
   (`ClaveInicial2026!`), sin cambio neto.
8. `DOVELA_WA` (rol Team) no puede llegar a `/usuarios` por URL directa — redirige a `/`.

**Nota:** la contraseña de `DOVELA_LG` documentada en `ESTADO_PROYECTO.md`
(`DovelaScrum2026!`) ya no es válida — el usuario la cambió en algún momento a
`Latitude123$` (misma que la de la base de datos Postgres, casualidad o reutilización
deliberada de contraseña; no se investigó). Queda actualizada abajo.

`ESTADO_PROYECTO.md` actualizado marcando la migración de rutas como verificada.

## Pendiente para después de esta sesión

Trackear en git los assets nuevos que aparecieron sin seguimiento: `00_ARCHIVOS/FondoDia.png`,
`00_ARCHIVOS/FondoNoche.png`, `00_ARCHIVOS/LOGO_DOVELA/` (usados por los commits de fondo/logo
del 2026-08-24 pero nunca agregados al repo).

## Credenciales actualizadas

`DOVELA_LG` (Scrum Master) → `Latitude123$` (reemplaza la `DovelaScrum2026!` documentada
anteriormente, que ya no funciona).
